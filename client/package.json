{
  "name": "mini-notes-client",
  "version": "1.0.0",
  "description": "Mini Notes PWA - Client",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:clean": "rm -rf node_modules/.vite && vite",
    "kill": "lsof -ti:5173 | xargs kill -9 || true",
    "start": "npm run kill && npm run dev:clean",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
